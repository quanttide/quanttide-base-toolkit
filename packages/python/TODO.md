# TODO

## 待办

- [ ] 补充字段模块的测试覆盖（`tests/test_fields.py`）

## 已完成

- [x] 标准字段模型：8 个确认字段实现为 Pydantic 字段类
- [x] 补充 `[project.urls]`、`keywords` 等 PyPI 元信息
- [x] 修复 `pyproject.toml` readme 路径
- [x] 补充测试覆盖 `PermissionError` 和 `OSError` 异常分支
- [x] 环境变量测试改用 `monkeypatch` fixture
